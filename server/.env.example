# PostgreSQL connection string
DATABASE_URL="postgres://postgres:jaimaakali@localhost:5433/ecommerce"

# Auth
JWT_SECRET="replace-with-a-long-random-secret-at-least-32-chars"
JWT_EXPIRES_IN="7d"

# Server
PORT=3001
NODE_ENV=development

# Optional: comma-separated origins for CORS (default allows localhost Vite)
# CLIENT_URL=http://localhost:5173
